[build-system]
requires = ["setuptools>=82,<83", "wheel>=0.46,<0.47"]
build-backend = "setuptools.build_meta"

[project]
name = "quantbt-engine"
version = "1.0.10"
description = "Transparent, high-performance quantitative backtesting engine"
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = "MIT"
authors = [
    { name = "Vu Cong Minh", email = "vugioan11022002@gmail.com" },
]
keywords = [
    "backtesting",
    "quant",
    "trading",
    "numba",
    "portfolio",
    "event-driven",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Financial and Insurance Industry",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business :: Financial :: Investment",
    "Topic :: Scientific/Engineering",
    "Typing :: Typed",
]
dependencies = [
    "numpy>=2.2.6,<2.3",
    "pandas>=2.3.3,<2.4",
    "numba>=0.65.1,<0.66",
    # Supported Linux consumers receive a pre-built PyO3 companion. Other
    # platforms retain the Python/Numba core without source compilation.
    "quantbt-native==0.4.1; sys_platform == 'linux' and platform_machine == 'x86_64' and implementation_name == 'cpython' and python_version >= '3.11' and python_version < '3.14'",
]

[project.optional-dependencies]
optimization = [
    "optuna>=4.8.0,<4.9",
    "arch>=8.0.0,<8.1",
    "scikit-learn>=1.8.0,<1.9",
]
reports = [
    "quantstats==0.0.81",
]
viz = [
    "matplotlib>=3.10.9,<3.11",
    "seaborn>=0.13.2,<0.14",
]
validation = [
    "nautilus-trader>=1.230.0,<1.231; python_version >= '3.12'",
]
# Backward-compatible extra name. The bounded native companion is a direct,
# platform-marked dependency on supported Linux, so this extra adds nothing.
native = []
all = [
    "optuna>=4.8.0,<4.9",
    "arch>=8.0.0,<8.1",
    "scikit-learn>=1.8.0,<1.9",
    "quantstats==0.0.81",
    "matplotlib>=3.10.9,<3.11",
    "seaborn>=0.13.2,<0.14",
    "nautilus-trader>=1.230.0,<1.231; python_version >= '3.12'",
]

[project.urls]
Homepage = "https://github.com/BobbyAxerol/quantbt"
PyPI = "https://pypi.org/project/quantbt-engine/"
Repository = "https://github.com/BobbyAxerol/quantbt"
Issues = "https://github.com/BobbyAxerol/quantbt/issues"
Documentation = "https://github.com/BobbyAxerol/quantbt/tree/main/docs"
Changelog = "https://github.com/BobbyAxerol/quantbt/blob/main/CHANGELOG.md"

[dependency-groups]
dev = [
    "pytest>=9.1,<10.0",
    "pytest-cov>=7.0,<8.0",
    "hypothesis>=6.148,<7.0",
    "ruff>=0.14,<0.15",
    "mypy>=1.19,<2.0",
    "build>=1.3,<2.0",
    "twine>=6.2,<7.0",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["quantbt*"]

[tool.setuptools.package-data]
quantbt = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.uv]
package = true

[tool.uv.sources]
# Phase 55A lock/build source. This local override is never emitted into the
# public wheel metadata; consumers resolve the exact PyPI requirement above.
quantbt-native = { path = "rust/native_event" }
